const NavBar = () => {
  return (
    <nav className="nav">
      <section className="nav-bar__brand">
        <span className="nav-bar__brand__name">
          Packify<span className="dot">.</span>me
        </span>
      </section>
    </nav>
  );
};

export default NavBar;
